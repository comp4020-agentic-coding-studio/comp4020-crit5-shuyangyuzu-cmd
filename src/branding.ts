// The canonical site title, used for the document <title>, the nav
// wordmark, and the landing-screen heading. Written with the actual
// Unicode ellipsis (not the &hellip; entity) so it renders correctly
// wherever it's interpolated, including inside an Astro string prop.
export const APP_TITLE = "GOING, GOING…";
